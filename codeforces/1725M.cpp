#include<bits/stdc++.h>

#define N 100000+7
#define M 200000+7

using namespace std;

int main(){
    int n, m;
    cin >> n >> m;
    int u[M], v[M], w[M];
    for (int i = 1; i <= n; ++i) {
        cin >> u[i] >> v[i] >> w[i];
    }
}