#include<bits/stdc++.h>

using namespace std;

int board[207][207];

diagonal_sum(int n, int m){
    for(int i = )
}

int main(){
    int t;
    cin >> t;
    while(t--){
        int n, m;
        cin >> n >> m;
        for (int i = 1; i <= n; ++i) 
            for (int j = 1; j <= m; ++j)
                cin >> board[i][j];
        diagonal_sum(n, m);
    }
}